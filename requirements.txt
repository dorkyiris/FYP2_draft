# Pose estimation (HRNet via mmpose)
# mmcv/mmdet/mmpose MUST be installed via mim, NOT pip — mim fetches pre-built wheels.
# Run once after pip install -r requirements.txt:
#   pip install openmim
#   mim install mmengine
#   mim install "mmcv>=2.0.0"
#   mim install mmdet
#   mim install mmpose
openmim

# Core API
fastapi>=0.111,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.7,<3.0

# Rehabilitation engine
numpy>=1.26,<2.0
pyyaml>=6.0,<7.0
opencv-python-headless>=4.9,<5.0

# Testing
pytest>=8.0,<10.0
pytest-cov>=5.0,<7.0
httpx>=0.27,<1.0
pandas>=2.1,<3.0
